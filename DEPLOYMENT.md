# RD-Agent 部署指南

## 🚀 部署概述

RD-Agent现在支持完整的CI/CD流水线，包括：
- 本地开发环境部署
- Docker容器化部署
- 生产环境自动化部署
- 多环境配置管理

## 📦 部署方式

### 1. 本地开发环境

```bash
# 克隆仓库
git clone https://github.com/afanty2021/RD-Agent.git
cd RD-Agent

# 安装依赖
pip install -e .

# 启动应用
python rdagent ui --port 8000
```

### 2. Docker部署

```bash
# 构建镜像
docker build -t rdagent:latest .

# 运行容器
docker run -d --env-file .env.prod -p 8000:8000 rdagent:latest
```

### 3. 生产环境部署

```bash
# 使用部署脚本
./scripts/deploy.sh

# 或手动部署
docker run -d --env-file .env.prod -p 8000:8000 rdagent:latest
```

## 🔧 环境配置

### 开发环境变量
```bash
export OPENAI_API_KEY=your_api_key_here
export PYTHONPATH=/path/to/rdagent
```

### 生产环境变量
见 `.env.prod` 文件

## 📋 部署检查清单

- [ ] Docker镜像构建成功
- [ ] 容器运行正常
- [ ] 端口可访问
- [ ] 健康检查通过
- [ ] 日志输出正常
- [ ] 环境变量加载正确

## 🛠️ 故障排除

1. **端口冲突**
   ```bash
   # 检查端口占用
   lsof -i :8000
   ```

2. **容器启动问题**
   ```bash
   # 查看容器日志
   docker logs rdagent-container
   ```

3. **权限问题**
   ```bash
   # 检查文件权限
   ls -la /app
   ```

## 📚 部署命令参考

```bash
# 开发环境快速部署
make dev

# 测试环境部署
make test

# 生产环境部署
make prod

# Docker环境部署
make docker

# 清理
make clean
```

## 🔄 维护和更新

### 定期更新
- 每月更新依赖包
- 每季度更新Docker基础镜像
- 每次发布后检查性能指标

### 监控和告警
- 使用Sentry进行错误监控
- 使用Prometheus进行性能监控
- 设置日志轮转和告警机制